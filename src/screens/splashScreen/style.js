import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#358cc3',
      justifyContent: 'center',
      alignItems: 'center',
    },
    splashTxt: {
      textAlign: 'center',
      color: 'white',
      fontSize: 24,
    },
    mainScreenTxt: {
      textAlign: 'center',
      color: 'white',
      fontSize: 24,
    },
  });

  export default styles